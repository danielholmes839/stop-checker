query LocationSearch($location: LocationInput!) {
  searchStopLocation(location: $location, radius: 1000) {
    distance
    stop {
      id
      name
      code
      location {
        latitude
        longitude
      }
    }
  }
}
